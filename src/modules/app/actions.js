// import { createAction } from 'redux-action';
// import * as types from './types';

// export const getUserStart = createAction(types.GET_USE_START);
// export const getUserSuccess = createAction(types.GET_USE_SUCCESS);
// export const getUserError = createAction(types.GET_USE_ERROR);
